# Source-preview delivery record

This record follows the frozen package build and its bundled preparation reports.
It updates delivery state without changing or rerunning the final engineering
evaluation. The final patient evaluation remains bound to payload
`e84bef376703e8029ac673cfbc1f85d6cbc5fbb1db608367ca221e7e4c22150b`.
The subsequent macOS CPU fix has payload
`d775eaee01cf4df9f08aba72b75f13aba868f409c5d59eab2557629e9b0a36fd`
and its own development and hosted verification.

## Owner decision and source identity

On 6 September the owner approved the prepared source-preview commit, push,
draft PR and two required CI checks, while deferring registry publication and
model redistribution. The exact decision and approved artifact identities are
retained in `benchmark/results/pro-completion-20260906/owner-decision-20260906.json`.
Unresolved model-rights questions remain recorded; source-preview approval is
not an unrestricted inference/model-distribution grant.

The initial source was pushed in [draft PR #1](https://github.com/alexprotonotarios/ecg-waveparse/pull/1)
at commit `901c70a1a1b203ab0cff68c082682c4fc6fb7c1c`, based on public main
`bbf1dd38705cf38588d2bc521493528b0833bbc4`. All 252 committed files match the
approved audited source tree, SHA-256
`8c779b48001d446ac3448c163a4233b7695746b808913872c0c54bc57a193557`.
The committed change includes the two intended removals of UI-only helpers
that were already staged during preparation: 174 changed files in total.
Private development history, patient files and model weights are absent.

The PR workflow checks out GitHub's integration commit
`8936d49086c6dc2837e1cf1316dd83839c400041`. Its complete Git tree is identical
to the candidate's: `1ccecbb8c77d7e5a6de0ba7d91c37f82445bd4cf`.
The checkout identity is not being conflated with the candidate commit.

## Required checks and initial hosted attempt

Fresh API readback confirms that main requires `Fast package contracts` and
`Supported platform equivalence`, bound to the observed GitHub Actions app
15368, with strict up-to-date checking. Administrator enforcement remains
disabled, preserving the existing administrator behavior. No review requirement
or push restriction existed before the update, and none was introduced or
removed. This does not claim that repository administrators cannot bypass checks.

Automatic approval review rejected an initial request that also enabled
administrator enforcement. A narrower request preserving that setting was
accepted and reloaded successfully. The response and request receipts remain
in the local evidence directory.

[Hosted run 34055758855](https://github.com/alexprotonotarios/ecg-waveparse/actions/runs/34055758855)
has passed 108 package tests and 92 Python checks in its fast tier. The Linux
job completed successfully in 37 minutes, including installed consumers,
eight real CPU extraction attempts, 66/66 library gates and the lifecycle smoke.
Seven attempts returned quantitative candidates requiring review; one abstained
and none failed at runtime. All seven returned cases passed strict semantics.
Conditional pooled RMSE was 30.143 µV across 193,961 compared samples, mean case
correlation 0.95081 and mean coverage 99.3%. These are synthetic regression
results, not reference-qualified clinical outputs.

The initial macOS job was cancelled at its 90-minute cap (90m49s observed job
duration). Five cases returned, the sixth was interrupted and the remaining two
did not start. The partial report retains the full planned denominator of eight.
Several 2200-pixel candidates repeatedly failed near their 180-second deadlines,
changing the selected output: clean-case RMSE was 74.640 µV on macOS versus
16.371 µV on Linux. The aggregate correctly failed its prerequisite check;
that attempt produced no passing platform-equivalence result. Its overall conclusion
is `cancelled`. The CPU investigation below addresses this failure; neither increasing
the workflow timeout nor changing fidelity limits would resolve the drift.

The common fixture artifact's GitHub digest matches the downloaded ZIP. All
24 image/truth/annotation hashes verify across eight distinct cases from one
deterministic signal family. This is regression evidence, not eight independent
clinical recordings.

The downloaded Linux npm and wheel archives are byte-identical to the approved
local artifacts. The full source archive contains exactly the same 252 approved
files, although its compressed archive bytes differ. The Python source
distribution differs only in `.gitignore`, inherited from the minimal public
checkout rather than the application workspace. Rebuilding that downloaded
source distribution produces the identical approved wheel. All 37 runtime-file
hashes verify in both language distributions. Receipts and downloaded archives
remain under `benchmark/results/pro-completion-20260906/`; packaging metadata
differences are not being represented as byte identity.

The partial macOS artifact was also downloaded and verified. Its npm and wheel
archives are byte-identical to the approved ones; all 252 source files match.
Its Python source distribution is identical to the Linux source distribution
already rebuilt into the approved wheel. Package identity is therefore verified
for both platforms, while runtime parity remains failed/incomplete.

## macOS CPU correction and second hosted run

The authorized draft PR now points to
`693f6bf5d024603ca08cfaff2686e8e4e0a1c071`. Remote push and PR head were
independently confirmed; the public checkout is clean. This adds eleven selected
source/test/report files over the initial candidate (607 insertions, 13 deletions),
with no private history or ECG/model files.

[The CPU correction](MACOS-CPU-2026-09-06.md) bounds individual convolution
workspaces and disables the Apple large-allocation cache only in the macOS CPU
inference child. The actual-helper controlled forward passed the fixed gates at
21.43 seconds and 3.93 GiB, versus 145.75 seconds and 8.74 GiB for the original.
The full normalization/model graph, resolution, weights, candidate time limits
and numerical gates are unchanged. Earlier failed probe variants are retained.

The exact new artifacts passed 109 package contracts, 93 fast Python checks,
14 native wrapper checks and fresh installed npm/Python consumer verification.
The source audit covers 255 files and both language distributions verify all
38 runtime-file hashes. Local artifact identities and source membership are
retained in `cpu-fix-prepared-artifact-identities-v1.json` under the evidence
folder, with a corresponding-source identity of
`b61e409688510d2063d1086f0893c65b011a5b5dff7208d2ca7e182c66e16dc1`
(SHA-256 of the sorted compact path/hash JSON).

[Hosted run 34062261795](https://github.com/alexprotonotarios/ecg-waveparse/actions/runs/34062261795)
checks out integration commit `e320f1356cd093b79fe5ea96b4232f4e5172d449`,
whose complete Git tree matches the candidate's
`f816e422e6bea78c109b9d20a5ccc1d45683b56c`. **All four hosted jobs passed:**
fast contracts, Linux packages, macOS packages and supported-platform equivalence.
Fresh PR readback confirms that all four checks are successful on the exact
draft head; main still requires the two approved Actions checks. The hosted
aggregate was independently reproduced from the downloaded compact evidence,
with every comparison field and the resulting JSON bytes identical.

The completed local installed regression attempted all eight cases: seven
returned candidates, one abstained and none failed at runtime. All 63 candidate
attempts completed (maximum 35.45 seconds), all seven returned cases passed
strict semantics and the library gates passed 66/66. Conditional pooled RMSE
was 30.241 µV across 193,978 samples, mean case correlation 0.95078 and coverage
99.3%. Profiled wall time was 16.85 minutes, with 5.40 GiB peak sampled
process-tree RSS and 0.211 GiB workspace. The controlled-forward 5 GiB
investigation gate is not a universal full-pipeline memory cap. Installed
review/compaction/tamper/recovery checks and Python readback of all eight
JavaScript results passed. Cancellation at source admission took 71.1 ms;
this particular smoke is not mid-neural cancellation evidence.

Fresh hosted Linux completed all eight cases with seven returns, one abstention,
zero runtime failures and all 63 candidate attempts completed. Strict semantics
passed 7/7 and library gates 66/66. Its conditional RMSE was 30.143 µV across
193,961 samples, correlation 0.95081 and coverage 99.3%; sampled process-tree
RSS was 8.56 GiB and extraction wall time 32.50 minutes. The downloaded npm,
wheel and sdist are byte-identical to the approved artifacts. All 255 source
files and 38 runtime files per language match; the source-archive bytes
differ while its file contents match. The same-payload comparison between these hosted Linux results
and the local Mac results passes all eight cases under unchanged limits.
The required comparison between both hosted jobs also passes all eight cases.

Hosted macOS completed all eight cases: seven returns, one abstention, zero
runtime failures, all 63 candidates completed, 7/7 strict semantic passes and
66/66 library gates. Conditional pooled RMSE was 30.265 µV across 194,006
samples, mean case correlation 0.95079 and coverage 99.3%. Extraction took
57.09 minutes and the complete job took 60m05s. The slowest candidate took
170.41 seconds against the unchanged 180-second deadline. This is successful
completion with limited timing headroom, not a claim of low latency on every Mac.

The Mac profiler recorded 3,000 samples, an observed process-tree RSS maximum
of 4.98 GiB and 0.211 GiB workspace. It also recorded `TimeoutExpired` while
sampling; the observed RSS is incomplete peak evidence and not a certified
memory bound. Source-admission cancellation took 129.1 ms; review, compaction,
tamper refusal and dead-owner recovery passed. The Mac npm/wheel/sdist bytes
are identical to the approved archives; all 255 source files and all 38 runtime
files per language verify. The source-archive bytes differ while its file
contents match.

The aggregate uses the same payload, runtime, scorer and source/truth bytes.
All seven returned waveform/uncertainty comparisons pass; the eighth verifies
matching abstention. Maximum cross-platform waveform RMSE is 10.157 µV against
the fixed 25 µV limit, with all missingness, truth-error, correlation, coverage,
policy and uncertainty limits unchanged. These eight cases share one
deterministic signal family, not eight independent clinical recordings.

The final receipt is
`benchmark/results/pro-completion-20260906/cpu-fix-hosted-final-verification.json`.
The initial failed hosted attempt remains in the record. Public bundled reports
retain their preparation snapshots; this local record and the updated draft PR
body describe final delivery without changing the tested source commit.

## Boundaries retained

No merge, registry publication, production deployment or runtime activation has
been performed. The final independent engineering results remain 19/25 returns,
six abstentions and 4/25 reference-qualified cases, with a failed 6 GiB sampled
memory budget. Passing CI cannot turn those failures into fidelity passes or
clinical validation. The source preview remains a draft.
