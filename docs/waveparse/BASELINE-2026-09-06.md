# Frozen engineering baseline, 6 September 2026

All 8 attempts completed: 7 quantitative outputs requiring review, 1 policy
abstention and 0 infrastructure failures. No output was human accepted.
Quantitative-output yield is 7/8 (87.5%). This is eight images from one
deterministic waveform family, not an independent patient benchmark.

The frozen local commit is `9a020c4f06d4dfdfbf59d0cb18660f50671b1e39`.
Its package was built from an untouched `git archive` snapshot and installed
into a separate consumer. Source, fixture, truth, model/runtime, scorer and
returned-asset identities are retained under
`benchmark/results/gpt6pro-20260906/baseline/`, with a keep marker. The public
[baseline receipt](verification/2026-09-06-baseline.json) contains the artifact
identities, exact denominator, candidate outcomes and scores.

| Case | Outcome | RMSE, µV | Correlation | Coverage |
| --- | --- | ---: | ---: | ---: |
| 6×2 clean | needs_review | 16.21 | 0.9912 | 99.95% |
| 6×2 V1 arrow | needs_review | 16.10 | 0.9912 | 98.85% |
| 6×2 notch | needs_review | 15.83 | 0.9920 | 99.91% |
| 6×2 low resolution | needs_review | 26.38 | 0.9756 | 99.99% |
| 6×2 blur | needs_review | 28.27 | 0.9720 | 99.96% |
| 6×2 perspective | needs_review | 18.51 | 0.9882 | 99.49% |
| 3×4 clean | needs_review | 74.45 | 0.8094 | 97.09% |
| 12×1 clean | abstention | — | — | — |

The original scorer is version 5: these are lead-local aligned metrics, not
strict canonical-placement validation. Among returned outputs, global RMSE
is 28.62 µV over 194,007 compared samples; mean case correlation is 0.959945
and mean case coverage 99.319%. Sequential wall time was 810.254 seconds on
macOS arm64/MPS. These observations do not establish CPU/platform equivalence.

The unchanged historical gates still fail **2 of 32** checks: low-resolution
and perspective outcomes differ from the old expected refusals. The separate
library/review contract passes **66 of 66** assertions. Neither comparison has
been relabelled or substituted for the other. See the receipt for each result.

## Stable failure inventory

| Failure ID | Cases | Category | Evidence / required follow-up |
| --- | --- | --- | --- |
| BASE-001 | low_resolution | Morphology / source resolution | Narrow V3 notch is lost; source and diagnostic overlay retained. Distinguish source recoverability from decoded morphology. |
| BASE-002 | 3x4 clean | Trace / morphology | Correlation 0.8094 and morphology event recall 2/3. Per-lead scores and overlay retained. |
| BASE-003 | 12x1 clean | Policy abstention | No quantitative export; retain source, diagnostic candidates' summaries and terminal publication reason. |
| BASE-004 | low_resolution, perspective | Historical policy expectation | Two unchanged historical status mismatches, not infrastructure failures. |
| BASE-005 | all returned outputs | Evaluation semantics | Scorer v5 selects the most-supported panel for local metrics. Strict placement/segment identity was not established by those scores. |
| BASE-006 | all returned outputs | Evidence lifecycle | Frozen retention v1 removes uncertainty/segments after review. This campaign leaves the baseline unreviewed and pins its evidence; policy v2 fixes subsequent runs. |

Each case has one terminal outcome even when several failure IDs apply. The
local `evidence-index.json` binds failure inspection to source/overlay/signal/
uncertainty bytes. The baseline source and private repository history are never
added to the public source allowlist.

## Reproduction

Follow [REGRESSION.md](REGRESSION.md) from the frozen snapshot using its installed
consumer and the saved fixture manifests. A fresh output directory is required.
Reusing the saved input bytes permits direct comparison; regenerating on another
platform can change font/raster bytes and requires a separately reported run.
Compare the historical and library gate files independently. Subsequent scorer
v6 results must be labelled separately from these unchanged v5 measurements.
